# Projects to Add: Detailed List

This document contains the detailed project configurations for Phase 1 (25→50) and Phase 2 (50→100).

---

## Phase 1: Projects 26-50 (Priority)

### 26. Chatwoot - Customer Support
```python
"chatwoot": RealProjectConfig(
    repos=("chatwoot/chatwoot",),
    team_name="Chatwoot",
    team_slug="chatwoot-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="CHAT",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Open source Intercom/Zendesk alternative (21k stars)",
),
```

### 27. Medusa - E-Commerce
```python
"medusa": RealProjectConfig(
    repos=("medusajs/medusa",),
    team_name="Medusa",
    team_slug="medusa-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="MED",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Open source Shopify alternative (26k stars)",
),
```

### 28. Appsmith - Internal Tools
```python
"appsmith": RealProjectConfig(
    repos=("appsmithorg/appsmith",),
    team_name="Appsmith",
    team_slug="appsmith-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="APP",
    ai_base_adoption_rate=0.50,
    survey_response_rate=0.55,
    description="Open source Retool alternative (35k stars)",
),
```

### 29. n8n - Workflow Automation
```python
"n8n": RealProjectConfig(
    repos=("n8n-io/n8n",),
    team_name="n8n",
    team_slug="n8n-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="N8N",
    ai_base_adoption_rate=0.55,
    survey_response_rate=0.55,
    description="Open source Zapier alternative (50k stars)",
),
```

### 30. Strapi - Headless CMS
```python
"strapi": RealProjectConfig(
    repos=("strapi/strapi",),
    team_name="Strapi",
    team_slug="strapi-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="STRA",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Leading open source headless CMS (65k stars)",
),
```

### 31. AFFiNE - Knowledge Management
```python
"affine": RealProjectConfig(
    repos=("toeverything/AFFiNE",),
    team_name="AFFiNE",
    team_slug="affine-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="AFF",
    ai_base_adoption_rate=0.55,
    survey_response_rate=0.55,
    description="Open source Notion alternative (43k stars)",
),
```

### 32. Dify - AI/LLM Platform
```python
"dify": RealProjectConfig(
    repos=("langgenius/dify",),
    team_name="Dify",
    team_slug="dify-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="DIFY",
    ai_base_adoption_rate=0.70,
    survey_response_rate=0.55,
    description="Open source LLM app platform (58k stars)",
),
```

### 33. PocketBase - Database/BaaS
```python
"pocketbase": RealProjectConfig(
    repos=("pocketbase/pocketbase",),
    team_name="PocketBase",
    team_slug="pocketbase-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="PB",
    ai_base_adoption_rate=0.35,
    survey_response_rate=0.55,
    description="Backend in 1 file - Go (42k stars)",
),
```

### 34. Plausible - Analytics
```python
"plausible": RealProjectConfig(
    repos=("plausible/analytics",),
    team_name="Plausible",
    team_slug="plausible-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="PLA",
    ai_base_adoption_rate=0.35,
    survey_response_rate=0.55,
    description="Privacy-first Google Analytics alternative (20k stars)",
),
```

### 35. SigNoz - Observability
```python
"signoz": RealProjectConfig(
    repos=("SigNoz/signoz",),
    team_name="SigNoz",
    team_slug="signoz-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="SIG",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Open source DataDog alternative (19k stars)",
),
```

### 36. GrowthBook - Feature Flags
```python
"growthbook": RealProjectConfig(
    repos=("growthbook/growthbook",),
    team_name="GrowthBook",
    team_slug="growthbook-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="GB",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Open source feature flags & A/B testing (6k stars)",
),
```

### 37. Huly - Project Management
```python
"huly": RealProjectConfig(
    repos=("hcengineering/huly", "hcengineering/platform"),
    team_name="Huly",
    team_slug="huly-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="HULY",
    ai_base_adoption_rate=0.50,
    survey_response_rate=0.55,
    description="All-in-one project management platform (15k stars)",
),
```

### 38. Erxes - CRM
```python
"erxes": RealProjectConfig(
    repos=("erxes/erxes",),
    team_name="Erxes",
    team_slug="erxes-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="ERX",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Open source HubSpot alternative (3.5k stars)",
),
```

### 39. Mattermost - Communication
```python
"mattermost": RealProjectConfig(
    repos=("mattermost/mattermost", "mattermost/mattermost-server"),
    team_name="Mattermost",
    team_slug="mattermost-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="MM",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Open source Slack alternative (31k stars)",
),
```

### 40. Saleor - E-Commerce
```python
"saleor": RealProjectConfig(
    repos=("saleor/saleor",),
    team_name="Saleor",
    team_slug="saleor-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="SAL",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="GraphQL-first e-commerce platform (21k stars)",
),
```

### 41. ToolJet - Internal Tools
```python
"tooljet": RealProjectConfig(
    repos=("ToolJet/ToolJet",),
    team_name="ToolJet",
    team_slug="tooljet-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="TJ",
    ai_base_adoption_rate=0.50,
    survey_response_rate=0.55,
    description="Open source Retool alternative (32k stars)",
),
```

### 42. Directus - Headless CMS
```python
"directus": RealProjectConfig(
    repos=("directus/directus",),
    team_name="Directus",
    team_slug="directus-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="DIR",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Open Data Platform / Headless CMS (29k stars)",
),
```

### 43. Typebot - Forms/Chatbot
```python
"typebot": RealProjectConfig(
    repos=("baptisteArno/typebot.io",),
    team_name="Typebot",
    team_slug="typebot-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="TYPE",
    ai_base_adoption_rate=0.50,
    survey_response_rate=0.55,
    description="Conversational form builder (7k stars)",
),
```

### 44. Appwrite - BaaS
```python
"appwrite": RealProjectConfig(
    repos=("appwrite/appwrite",),
    team_name="Appwrite",
    team_slug="appwrite-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="AW",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Open source Firebase alternative (46k stars)",
),
```

### 45. Ghost - Publishing
```python
"ghost": RealProjectConfig(
    repos=("TryGhost/Ghost",),
    team_name="Ghost",
    team_slug="ghost-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="GHO",
    ai_base_adoption_rate=0.35,
    survey_response_rate=0.55,
    description="Professional publishing platform (48k stars)",
),
```

### 46. Zitadel - Identity
```python
"zitadel": RealProjectConfig(
    repos=("zitadel/zitadel",),
    team_name="Zitadel",
    team_slug="zitadel-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="ZIT",
    ai_base_adoption_rate=0.40,
    survey_response_rate=0.55,
    description="Identity management platform (9k stars)",
),
```

### 47. Flowise - AI/LLM
```python
"flowise": RealProjectConfig(
    repos=("FlowiseAI/Flowise",),
    team_name="Flowise",
    team_slug="flowise-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="FLO",
    ai_base_adoption_rate=0.70,
    survey_response_rate=0.55,
    description="Visual LLM chatbot builder (33k stars)",
),
```

### 48. Outline - Knowledge Management
```python
"outline": RealProjectConfig(
    repos=("outline/outline",),
    team_name="Outline",
    team_slug="outline-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="OUT",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Team knowledge base wiki (29k stars)",
),
```

### 49. Budibase - Internal Tools
```python
"budibase": RealProjectConfig(
    repos=("Budibase/budibase",),
    team_name="Budibase",
    team_slug="budibase-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="BUDI",
    ai_base_adoption_rate=0.50,
    survey_response_rate=0.55,
    description="Low-code internal tools platform (23k stars)",
),
```

### 50. Webstudio - Website Builder
```python
"webstudio": RealProjectConfig(
    repos=("webstudio-is/webstudio",),
    team_name="Webstudio",
    team_slug="webstudio-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="WEB",
    ai_base_adoption_rate=0.45,
    survey_response_rate=0.55,
    description="Open source Webflow alternative (5k stars)",
),
```

---

## Phase 2: Projects 51-100

### 51-60: AI/LLM & Knowledge Tools
```python
"openwebui": RealProjectConfig(
    repos=("open-webui/open-webui",),
    team_name="Open WebUI",
    team_slug="openwebui-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="OWUI",
    ai_base_adoption_rate=0.75,
    description="ChatGPT-like interface (55k stars)",
),
"ollama": RealProjectConfig(
    repos=("ollama/ollama",),
    team_name="Ollama",
    team_slug="ollama-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="OLLA",
    ai_base_adoption_rate=0.70,
    description="Local LLM runner (110k stars)",
),
"appflowy": RealProjectConfig(
    repos=("AppFlowy-IO/AppFlowy",),
    team_name="AppFlowy",
    team_slug="appflowy-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="AFL",
    ai_base_adoption_rate=0.50,
    description="Open source Notion alternative (58k stars)",
),
"logseq": RealProjectConfig(
    repos=("logseq/logseq",),
    team_name="Logseq",
    team_slug="logseq-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="LOG",
    ai_base_adoption_rate=0.40,
    description="Knowledge graph note-taking (33k stars)",
),
```

### 61-70: API & DevTools
```python
"bruno": RealProjectConfig(
    repos=("usebruno/bruno",),
    team_name="Bruno",
    team_slug="bruno-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="BRU",
    ai_base_adoption_rate=0.40,
    description="Opensource IDE for API exploration (28k stars)",
),
"rocketchat": RealProjectConfig(
    repos=("RocketChat/Rocket.Chat",),
    team_name="Rocket.Chat",
    team_slug="rocketchat-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="ROCK",
    ai_base_adoption_rate=0.40,
    description="Team communications platform (41k stars)",
),
"umami": RealProjectConfig(
    repos=("umami-software/umami",),
    team_name="Umami",
    team_slug="umami-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="UMA",
    ai_base_adoption_rate=0.35,
    description="Simple analytics alternative (23k stars)",
),
"grafana": RealProjectConfig(
    repos=("grafana/grafana",),
    team_name="Grafana",
    team_slug="grafana-demo",
    max_prs=150,
    max_members=50,
    days_back=90,
    jira_project_key="GRAF",
    ai_base_adoption_rate=0.40,
    description="Observability platform (65k stars)",
),
"openstatus": RealProjectConfig(
    repos=("openstatusHQ/openstatus",),
    team_name="OpenStatus",
    team_slug="openstatus-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="STAT",
    ai_base_adoption_rate=0.50,
    description="Open source status page (6k stars)",
),
```

### 71-80: E-Commerce & Low-Code
```python
"payload": RealProjectConfig(
    repos=("payloadcms/payload",),
    team_name="Payload CMS",
    team_slug="payload-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="PAY",
    ai_base_adoption_rate=0.45,
    description="TypeScript headless CMS (29k stars)",
),
"vendure": RealProjectConfig(
    repos=("vendure-ecommerce/vendure",),
    team_name="Vendure",
    team_slug="vendure-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="VEND",
    ai_base_adoption_rate=0.40,
    description="TypeScript headless commerce (6k stars)",
),
"windmill": RealProjectConfig(
    repos=("windmill-labs/windmill",),
    team_name="Windmill",
    team_slug="windmill-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="WIND",
    ai_base_adoption_rate=0.55,
    description="Scripts to internal apps (12k stars)",
),
"nocobase": RealProjectConfig(
    repos=("nocobase/nocobase",),
    team_name="NocoBase",
    team_slug="nocobase-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="NOCO",
    ai_base_adoption_rate=0.45,
    description="No-code/low-code platform (14k stars)",
),
"activepieces": RealProjectConfig(
    repos=("activepieces/activepieces",),
    team_name="Activepieces",
    team_slug="activepieces-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="ACT",
    ai_base_adoption_rate=0.50,
    description="Open source Zapier alternative (11k stars)",
),
```

### 81-90: Security & Monitoring
```python
"keycloak": RealProjectConfig(
    repos=("keycloak/keycloak",),
    team_name="Keycloak",
    team_slug="keycloak-demo",
    max_prs=150,
    max_members=50,
    days_back=90,
    jira_project_key="KEY",
    ai_base_adoption_rate=0.35,
    description="Identity and access management (24k stars)",
),
"supertokens": RealProjectConfig(
    repos=("supertokens/supertokens-core",),
    team_name="SuperTokens",
    team_slug="supertokens-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="SUPA",
    ai_base_adoption_rate=0.45,
    description="Open source authentication (13k stars)",
),
"langflow": RealProjectConfig(
    repos=("langflow-ai/langflow",),
    team_name="Langflow",
    team_slug="langflow-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="LANG",
    ai_base_adoption_rate=0.70,
    description="Visual LLM workflow builder (36k stars)",
),
"uptimekuma": RealProjectConfig(
    repos=("louislam/uptime-kuma",),
    team_name="Uptime Kuma",
    team_slug="uptimekuma-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="KUMA",
    ai_base_adoption_rate=0.35,
    description="Self-hosted monitoring tool (62k stars)",
),
```

### 91-100: Design & Productivity
```python
"penpot": RealProjectConfig(
    repos=("penpot/penpot",),
    team_name="Penpot",
    team_slug="penpot-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="PEN",
    ai_base_adoption_rate=0.40,
    description="Open source Figma alternative (35k stars)",
),
"excalidraw": RealProjectConfig(
    repos=("excalidraw/excalidraw",),
    team_name="Excalidraw",
    team_slug="excalidraw-demo",
    max_prs=200,
    max_members=50,
    days_back=90,
    jira_project_key="EXC",
    ai_base_adoption_rate=0.45,
    description="Virtual whiteboard (89k stars)",
),
"tldraw": RealProjectConfig(
    repos=("tldraw/tldraw",),
    team_name="tldraw",
    team_slug="tldraw-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="TLD",
    ai_base_adoption_rate=0.50,
    description="Infinite canvas SDK (38k stars)",
),
"immich": RealProjectConfig(
    repos=("immich-app/immich",),
    team_name="Immich",
    team_slug="immich-demo",
    max_prs=300,
    max_members=50,
    days_back=90,
    jira_project_key="IMM",
    ai_base_adoption_rate=0.50,
    description="Self-hosted Google Photos alternative (56k stars)",
),
"memos": RealProjectConfig(
    repos=("usememos/memos",),
    team_name="Memos",
    team_slug="memos-demo",
    max_prs=200,
    max_members=30,
    days_back=90,
    jira_project_key="MEM",
    ai_base_adoption_rate=0.40,
    description="Privacy-first note-taking (36k stars)",
),
```

---

## Industry Category Distribution (100 Projects)

| Category | Count | Example Projects |
|----------|-------|------------------|
| AI/LLM Tools | 12 | Dify, Flowise, Ollama, Open WebUI, LangChain, Langflow |
| Internal Tools & Low-Code | 8 | Appsmith, ToolJet, Budibase, Windmill, NocoBase |
| Knowledge Management | 6 | AFFiNE, AppFlowy, Outline, Logseq, Memos |
| Headless CMS | 6 | Strapi, Directus, Payload, Ghost, TinaCMS |
| E-Commerce | 5 | Medusa, Saleor, Vendure, Bagisto |
| Customer Support | 4 | Chatwoot, FreeScout, Zammad |
| Communication | 5 | Mattermost, Rocket.Chat, Zulip, Novu, Resend |
| Analytics & Observability | 7 | PostHog, Plausible, Umami, SigNoz, Grafana, OpenStatus |
| Project Management | 6 | Plane, Huly, Focalboard, OpenProject, Linear |
| Database & BaaS | 6 | Supabase, PocketBase, Appwrite, NHost, Neon |
| DevOps & Infrastructure | 6 | Coolify, Dokploy, CapRover, Vercel, Trigger.dev |
| Security & Identity | 6 | Infisical, Zitadel, Keycloak, SuperTokens, Comp AI |
| Workflow Automation | 5 | n8n, Activepieces, Huginn |
| Scheduling & Forms | 5 | Cal.com, Typebot, Formbricks, Rallly |
| CRM & Sales | 4 | Twenty, Erxes, Monica, Polar |
| Design & Creative | 5 | Penpot, Excalidraw, tldraw, Webstudio |
| API Testing | 3 | Hoppscotch, Bruno, Insomnia |
| Feature Flags | 3 | GrowthBook, Flagsmith, Unleash |
| Photo/Document Mgmt | 4 | Immich, Documenso, Paperless-ngx |

---

## Seeding Priority Order

**High Priority (Active, High Stars, Good AI Signal):**
1. Dify (58k stars, AI-native)
2. n8n (50k stars, automation leader)
3. Medusa (26k stars, e-commerce)
4. Appsmith (35k stars, internal tools)
5. AFFiNE (43k stars, knowledge)

**Medium Priority (Active, Moderate Stars):**
6-25: Strapi, SigNoz, Chatwoot, ToolJet, etc.

**Lower Priority (Smaller but Quality):**
26-50: GrowthBook, Webstudio, Erxes, etc.
